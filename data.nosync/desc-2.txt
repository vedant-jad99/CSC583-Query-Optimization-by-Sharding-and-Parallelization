Total 3 datasets:

1. A toy dataset, generated by team for testing small queries on prototype and phase 1 engines
2. A medium dataset (100000 documents) for benchmarking across all phases. Results included in the report.
	Usage: unzip the file input.7z to access the dataset
3. A large dataset (wikitext 103) of nearly 1M documents. Run the python script inside the data directory to download.
